import React from 'react'
import Shop from '../../pages/Shop'

// Products tab inside Marketplace — renders the full Shop page
const ProductsMode = () => {
  return <Shop />
}

export { ProductsMode }
export default ProductsMode
